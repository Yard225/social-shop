{
  "compilerOptions": {
    "baseUrl": ".", // ← indispensable
    "moduleResolution": "node",
    "paths": {
      /* générique : tous les paquets du mono-repo */
      "@shop/*": ["packages/*/src"],

      /* alias explicite, si besoin de pointer ailleurs */
      "@shop/shared-kernel": ["packages/shared-kernel/src"]
    }
  },
  "exclude": ["node_modules", "dist"]
}
